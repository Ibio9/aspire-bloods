# --- Core ---
NODE_ENV=development
PORT=4000
# APP_BASE_URL is also the CORS allow-origin in production — the frontend
# and API are separate deploys (Vercel + Railway), so this must be the
# exact frontend origin with no trailing slash, e.g.
# https://blood.aspireshield.com
APP_BASE_URL=http://localhost:5173
API_BASE_URL=http://localhost:4000

# --- Database (Railway provides DATABASE_URL automatically in production) ---
DATABASE_URL=postgresql://aspire:aspire_dev_password@localhost:5432/aspire_bloods

# --- Auth / sessions ---
JWT_ACCESS_SECRET=replace-with-64-char-random-hex
JWT_REFRESH_SECRET=replace-with-different-64-char-random-hex
ACCESS_TOKEN_TTL_MINUTES=15
REFRESH_TOKEN_TTL_DAYS=30
TRUSTED_DEVICE_TTL_DAYS=30
# In production this must be the shared parent domain — NO leading dot
# (e.g. blood.aspireshield.com, not .blood.aspireshield.com) — so the
# session cookie set by api.blood.aspireshield.com is also valid for
# blood.aspireshield.com. RFC 6265 domain-matching already includes
# subdomains without the dot; the old leading-dot syntax is unnecessary
# and at least one cookie library in this app's dependency tree rejects it
# outright ("option domain is invalid"), which crashes the request the
# first time a cookie is actually set (login/OTP-verify). The app refuses
# to boot in production with a leading dot here — see productionBootChecks.ts.
# Without this var set correctly at all, the cookie is scoped to the API
# subdomain only and the frontend never sees it. See DEPLOYMENT.md.
COOKIE_DOMAIN=localhost
CSRF_SECRET=replace-with-64-char-random-hex

# --- Field-level encryption (AES-256-GCM, 32-byte key, base64) ---
ENCRYPTION_KEY=replace-with-base64-32-byte-key

# --- Signed file URLs (local disk storage adapter) ---
FILE_SIGNING_SECRET=replace-with-64-char-random-hex
FILE_URL_TTL_MINUTES=10
# In production this must point at the mounted Railway volume (e.g.
# /data/storage), not the container's own ephemeral filesystem — anything
# written outside the volume is lost on the next deploy. See DEPLOYMENT.md.
STORAGE_ROOT=./storage

# --- Email (Resend) ---
# Required in production — the app refuses to boot without it. Without a
# real key, the email provider falls back to printing emails (including
# OTP codes and patient addresses) to the server console, which would put
# PII in Railway's logs.
RESEND_API_KEY=
EMAIL_FROM="Aspire Clinic <no-reply@aspireshield.com>"

# --- SMS (Twilio) — disabled by default, interface-only until enabled ---
SMS_ENABLED=false
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM_NUMBER=

# --- Clinical escalation routing (STAFF ONLY, never shown to a patient) ---
# Where a clinician is told that a released report came back with a result
# outside its reference range. The email names the patient and the flagged
# markers; it never carries a value. Production refuses to boot without a
# real address here — see productionBootChecks.ts.
#
# This is NOT the address patients are given. That is CLINIC_CONTACT_EMAIL
# below, and the two were one variable until August 2026: pointing the
# escalation at a named individual published their address in the portal
# sidebar of every screen and in the footer of every PDF.
ESCALATION_EMAIL=raheelmalik@me.com
ESCALATION_SMS_NUMBER=

# --- Patient-facing clinic contact details ---
# Shown in the patient portal's sidebar on every screen, and again beside
# anything out of range. A shared inbox rather than a person: it goes in front
# of everyone and outlives whoever is on the rota.
CLINIC_CONTACT_EMAIL=clinical-team@aspireshield.com
# CLINIC_PHONE is deliberately blank by default: the portal omits the phone row
# entirely until a real, answered number is set, rather than printing one that
# rings nowhere. The address comes from the code
# (apps/server/src/modules/content/clinicContact.ts).
CLINIC_PHONE=
CLINIC_HOURS=Monday to Friday, 9am – 5pm

# --- Admin access ---
# Comma-separated list of email addresses granted the ADMIN role. Parsed
# at boot (trimmed, lowercased, empties ignored) and re-checked on every
# request — there is no other way to become an admin anywhere in this
# codebase (no route, no setting, no seed script, no UI toggle). Required
# in production; the app refuses to boot without it. To change who has
# admin access in production: edit this variable in the Railway dashboard
# and redeploy — see DEPLOYMENT.md.
#
# /signup is open to everyone now (patients register themselves), which
# changes nothing here: an account created there is always stored as a
# patient, and the console appears only for addresses on this list, checked
# per request. Removing someone from this list demotes their existing
# account to patient-level access on their very next request; it does not
# lock them out or delete anything.
ADMIN_EMAILS=

# --- Randox lab integration ---
# Which adapter handles PDF-based results. Unrelated to the API integration
# below — an admin can still upload a Randox PDF with the API switched on.
LAB_ADAPTER=RANDOX_PORTAL

# =========================================================================
# Randox API integration (Nexus Lab + Clinic Booking)
# =========================================================================
# Two separate Azure-hosted APIs. Both use Azure B2C ROPC auth and both
# require a bearer token PLUS an Ocp-Apim-Subscription-Key header on every
# request, but they have different client ids, scopes, base URLs and
# subscription keys.
#
# STATUS, Aug 2026: BOTH DEVELOPER PORTAL ACCOUNTS ARE ACTIVE. What that
# settles and what it does not:
#
#   KNOWN, and defaulted below — both base URLs, both client ids, both
#   scopes, the shared token endpoint, the two booking service ids (787 UK /
#   788 ROI) and the sandbox test location (30, Clinic Location Crumlin).
#
#   STILL MISSING, and each blocks a live boot:
#     · RANDOX_NEXUS_SUBSCRIPTION_KEY and RANDOX_BOOKING_SUBSCRIPTION_KEY —
#       created in the developer portal, one per API.
#     · RANDOX_USERNAME / RANDOX_PASSWORD — the ROPC service account. Also
#       CREATED IN THE PORTAL rather than issued by Randox: both auth
#       documents say "This will be the username created within the
#       Developer Portal", so nobody is waiting to send them to us.
#     · RANDOX_CLINIC_ID — read from GET /Clinic/GetMyClinicDetails on the
#       first authenticated call.
#     · RANDOX_DEFAULT_TEST_REASON_ID and
#       RANDOX_DEFAULT_CANCELLATION_REASON_ID — from their own GET endpoints.
#     · RANDOX_ID_MAP_FILE — the agreed panel/test ids.
#     · RANDOX_CODE_MAP_FILE — THE VOID AND CAVEAT CODE LIST, which is the
#       one outstanding item NONE of the documents contains. It comes only
#       from the Randox Web Developer team. The boot guard refusing to start
#       live on the checked-in placeholder stays exactly as it is: without
#       the real list every genuine code classifies as unrecognised, which is
#       treated as void, which presents as "no results ever arrive".
#     · RANDOX_COLLECTION_METHODS — what we are contractually entitled to
#       offer.
#
# None of it is hardcoded anywhere in the app, and the server refuses to
# boot (naming each missing variable) if RANDOX_ENABLED=true and
# RANDOX_TRANSPORT=live without them.
#
# Until then: RANDOX_ENABLED=true with RANDOX_TRANSPORT=mock runs the whole
# order → book → poll → ingest flow against in-process fixtures.

# Master switch. False: no polling job, no ordering endpoints, no config
# validation — the integration is simply not there.
RANDOX_ENABLED=false

# mock  — the entire flow against fixtures; no credentials needed.
# live  — real Randox APIs.
# Refused in production with RANDOX_ENABLED=true: mock results would be
# ingested as though they were real patient results.
RANDOX_TRANSPORT=mock

# --- Base URLs ---
# Sandbox (stes-) by default. Switching to production is these two lines.
RANDOX_NEXUS_BASE_URL=https://stes-gpto-appapi-001-apim.azure-api.net/api/
RANDOX_BOOKING_BASE_URL=https://stes-cb-platform-apim.azure-api.net/booking-platform-api/

# --- Azure B2C client ids (documented, not secret) ---
RANDOX_NEXUS_CLIENT_ID=791f0001-20d7-4771-b4ab-359b4b9efd21
RANDOX_BOOKING_CLIENT_ID=0b0399a4-d61f-43fc-a0d0-3311f60cdcb1

# --- NOT ISSUED YET: subscription keys ---
# Sent as Ocp-Apim-Subscription-Key on every request to the respective API.
# Different key per API. Required for RANDOX_TRANSPORT=live.
RANDOX_NEXUS_SUBSCRIPTION_KEY=
RANDOX_BOOKING_SUBSCRIPTION_KEY=

# --- B2C scopes — CONFIRMED, from the auth documents AND both Postman
# collections (apps/server/src/modules/randox/specs/). Defaulted in code; set
# here only to override for production.
#
# COPY THESE CHARACTER FOR CHARACTER. The Nexus scope was wrong by ONE HYPHEN
# until Aug 2026 ("externalapi" for "external-api"), transcribed from the auth
# PDF's rendered text where the hyphen falls on a line break and disappears.
# A wrong scope means B2C issues no token at all, so every call 401s from the
# first one with a message about the token rather than about the scope. If you
# ever re-transcribe one, take it from the PDF's LINK TARGET or from the
# Postman collection, never from the paragraph.
RANDOX_NEXUS_SCOPE=https://randoxclinicbooking.onmicrosoft.com/gptestorderportal-external-api/User.Read.All
RANDOX_BOOKING_SCOPE=https://randoxclinicbooking.onmicrosoft.com/clinic-booking-platform-api/user_impersonation

# --- B2C token endpoint — CONFIRMED ---
# Both auth documents give the SAME tenant and policy (randoxclinicbooking /
# B2C_1_apim_ropc_signin1); only the client id and scope differ per API, so
# one shared value is correct. The per-API overrides remain for a production
# tenant that splits them.
RANDOX_B2C_TOKEN_URL=https://randoxclinicbooking.b2clogin.com/randoxclinicbooking.onmicrosoft.com/B2C_1_apim_ropc_signin1/oauth2/v2.0/token
RANDOX_NEXUS_TOKEN_URL=
RANDOX_BOOKING_TOKEN_URL=

# --- NOT ISSUED YET: service account credentials ---
# ROPC is a password grant, so it needs a real account. Set the shared pair
# if one account covers both APIs; use the per-API overrides otherwise.
# These are secrets — set them in Railway, never in a committed file.
RANDOX_USERNAME=
RANDOX_PASSWORD=
RANDOX_NEXUS_USERNAME=
RANDOX_NEXUS_PASSWORD=
RANDOX_BOOKING_USERNAME=
RANDOX_BOOKING_PASSWORD=

# --- NOT ISSUED YET: our clinic id ---
# An INTEGER on the wire (GetMyClinicDetails returns e.g. id: 146). Taken by
# CancelOrder and by both result endpoints. Without it no order can be
# attributed to this practice, so a live boot without it is refused.
# --- Clinic id: FETCHED, NOT CONFIGURED (Aug 2026) ---
# Leave this BLANK. It comes from GET /Clinic/GetMyClinicDetails on the boot
# sync and is read back out of the catalogue on a restart — the flow diagram
# names that endpoint as the authority for it three times over, once each for
# GetOrderStatus, GetOrderResultReports and GetOrderResultDetail. A typed-in id
# is a second source for a fact with one source, and a wrong clinic id on
# GetOrderResultDetail is a request for somebody else's order.
#
# It survives as an OVERRIDE, for pinning the value during a support session
# without a redeploy. It is no longer in the boot guard.
RANDOX_CLINIC_ID=
# The TestClinicLocationId CreatePendingOrder takes. The same value as the
# clinic id for a single-site clinic; a distinct id per site once a clinic
# has several (GetMyClinicDetails returns them under clinicTestLocations).
# Defaults to RANDOX_CLINIC_ID when blank.
RANDOX_TEST_CLINIC_LOCATION_ID=

# --- Clinic Booking service ids — KNOWN (Chris Caulfield, Aug 2026) ---
# The ServiceId for third-party in-clinic bookings. There are exactly two and
# no others. Not in any document; taken from email.
#
# The REGION picks between them, rather than the caller doing so per request:
# which country a booking is made in is a fact about this deployment, and an
# argument that could be either would eventually be the wrong one — a UK
# patient sent to service 788 is offered Irish clinics with nothing in the
# response to say the wrong service was asked for.
RANDOX_BOOKING_SERVICE_ID_UK=787
RANDOX_BOOKING_SERVICE_ID_ROI=788
RANDOX_BOOKING_REGION=UK

# SANDBOX TEST LOCATION, for the first smoke test: LocationId 30, "Clinic
# Location Crumlin", which Randox confirm has real availability. Not a
# variable — locations come from POST /Locations/GetServiceLocations — and
# recorded here because THE POSTMAN COLLECTION USES LocationId 15, WHICH MAY
# HAVE NO SLOTS AT ALL. An empty diary and a broken integration look identical
# from the outside, so start with 30.

# --- THE SANDBOX PASS DOES NOT READ THIS FILE ---
# `npm run sandbox:pass --workspace=apps/server` is standalone: it calls the
# stes- sandbox and writes the responses down, so it needs Randox credentials
# and nothing else — no DATABASE_URL, no JWT secrets, no app URLs, and not
# RANDOX_ENABLED or RANDOX_TRANSPORT either. Its three variables go in
# apps/server/.env.sandbox; see apps/server/.env.sandbox.example. The Clinic
# Booking key is optional there and the Nexus flow runs without it.

# --- CreatePendingOrder report flags (documented; defaulted in code) ---
# true asks Randox for the patient-facing "scalebar" report instead of the
# tabular laboratory report — that is the format of the example report in
# apps/server/src/modules/randox/specs/, and the one we would show a
# patient. GetOrderResultReports returns whichever was actually produced:
# the scalebar report if this was true and it generated, the lab report
# otherwise. It changes only the PDF, never the JSON results.
RANDOX_HEALTH_CHECK_PANEL_REPORT=true
# Asks Randox to calculate a cardiovascular risk score. It is derived from
# patient measurements (blood pressure, smoking, diabetes, ethnicity,
# lipids) that CreatePendingOrder has no fields for — those are supplied by
# CreateOrder/UpdateOrder at sample collection, which we do not use yet.
# Off until that flow exists, or it asks for a score with nothing to base
# it on.
RANDOX_CV_SCORE_REQUIRED=false

# --- NOT AGREED YET: the testing reason sent on every order ---
# The CreatePendingOrder schema requires a non-empty TestReasons array, so
# a live boot without this is refused — no order could be placed. Valid ids
# come from GET /TestReason/GetTestingReasons and are listed on the Randox
# catalogue admin screen once reference data has been refreshed.
RANDOX_DEFAULT_TEST_REASON_ID=
RANDOX_DEFAULT_TEST_REASON_DETAILS=Private health screening requested by the patient.

# --- NOT AGREED YET: the cancellation reason id ---
# CancelOrder takes a CancellationReasonId, not free text. Ids come from
# GET /CancellationReason/GetCancellationReasons; the documented example
# list is {1: Cancellation By Clinic, 2: Cancellation By Lab}. Our own
# free-text reason is still recorded locally for the audit trail.
RANDOX_DEFAULT_CANCELLATION_REASON_ID=

# How long cached Randox reference data (panels, tests, lookups, clinic
# details) is trusted before the admin screen flags it as out of date.
RANDOX_REFERENCE_DATA_TTL_MINUTES=720

# --- NOT CONFIRMED YET: permitted collection methods ---
# Comma-separated. Allowed values: IN_CLINIC, HOME_KIT, MOBILE_PHLEBOTOMY.
# NOTE: the Randox flow documentation describes only TWO collection methods
# — Home Sample Collection Kit and In-clinic. MOBILE_PHLEBOTOMY is not one
# of them; it is retained only because the enum already existed. Do not
# enable it without confirming Randox actually support it.
# Deliberately EMPTY: we do not yet have confirmation of what we are
# contractually entitled to offer, and placing an order by a route Randox
# have not agreed to is an order they will not fulfil. An order requesting
# a method not listed here is refused before anything is sent.
# Only IN_CLINIC uses the Clinic Booking API; the others have no slot.
RANDOX_COLLECTION_METHODS=

# --- NOT SUPPLIED YET: void / caveat code map ---
# JSON file mapping each Randox result code to VOID or CAVEAT.
#
#   VOID   -> the result is never shown to a patient as a value. Not greyed
#             out, not shown with a warning: no ReportResult row is written
#             at all, only an exclusion carrying a neutral "this test could
#             not be reported" note.
#   CAVEAT -> the result IS reported, with the code attached as metadata.
#             Visible to admins always; shown to patients only for codes
#             whose patientSafeNote has been filled in.
#
# A code that is NOT in this file is treated as VOID. That is the design,
# not a fallback: an unknown code is an unknown reason the lab flagged a
# number, and the only safe reading is "don't show it". Every unrecognised
# code is recorded and listed at GET /api/randox/unknown-codes, so the real
# list can be assembled from what actually arrives.
#
# The default below is a checked-in PLACEHOLDER with invented codes. A live
# boot still pointing at a .example.json file is refused.
RANDOX_CODE_MAP_FILE=./config/randox/result-codes.example.json

# --- NOT AGREED YET: panel / test id map ---
# JSON file mapping our catalogue keys to Randox's identifiers:
#   panels              our Panel.key  -> Randox panel/profile id
#   tests               our Marker.key -> Randox individual test id
#   markerNameOverrides Randox's marker name on a result -> our Marker.key
#                       (only where fuzzy name matching gets it wrong;
#                        anything unmapped is flagged for an admin in the
#                        ingestion log, never silently dropped)
# A live boot with no panels AND no tests is refused: CreatePendingOrder
# needs at least one, so no order could ever be placed.
RANDOX_ID_MAP_FILE=./config/randox/id-map.example.json

# --- Polling ---
# Randox recommend polling each outstanding order once per hour, staggered
# by order creation time. RANDOX_POLL_CRON is only how often the SWEEPER
# wakes to look for orders that are due; each order is still polled once per
# RANDOX_POLL_INTERVAL_MINUTES, on its own creation-time offset.
# Webhooks are coming; when they land, the webhook handler calls the same
# ingestion entry point and this schedule simply stops being needed.
RANDOX_POLL_CRON=*/5 * * * *
RANDOX_POLL_INTERVAL_MINUTES=60
# Orders per sweep, so a backlog spreads over several sweeps rather than
# hitting Randox in one burst.
RANDOX_POLL_BATCH_SIZE=25
# After this many consecutive failures an order stops being polled and waits
# for an admin, rather than being retried forever. It stays visible as
# stalled in GET /api/randox/orders.
RANDOX_POLL_MAX_FAILURES=12

# --- Talking to Randox politely, and surviving them being down ------------
# Which verb the EIGHT reference-data endpoints take. SETTLED: the OpenAPI
# document in modules/randox/specs/ is the source of truth and declares all
# eight of them GET with no parameters and no body. The rule for the whole API
# is one sentence — takes a body, POST; takes nothing, GET — and the nine POSTs
# are all under /Order. The "everything is POST" guidance was verbal and applied
# only to those nine.
# 'auto' survives as an escape hatch, not a hedge: it sends the declared verb
# and repeats ONCE as POST on a 404/405/501, then remembers which worked. Set it
# only if the sandbox contradicts its own spec.
RANDOX_REFERENCE_DATA_METHOD=get
# Whether to send an Azure B2C bearer alongside the subscription key.
# CONFIRMED REQUIRED (Aug 2026) — this is now a LEVER, not a hedge. The CB auth
# document says it in one sentence: "Authorisation will be the bearer token and
# in the header section include the following key: Ocp-Apim-Subscription-Key."
# Both, together, on every request, and both Postman collections send both. The
# Nexus OpenAPI document declaring only the key was a gap in that document:
# securitySchemes describes what the APIM gateway checks and the bearer is
# checked by the B2C policy in front of it.
# Leave it ON. Turning it off is a diagnostic step for a 401 you cannot explain,
# never a configuration. The subscription key is NOT switchable and always goes,
# in the HEADER: the query form would put a live credential in every access log
# between here and Randox.
RANDOX_BEARER_TOKEN_ENABLED=true
# Outbound pacing, PER API. A poll sweep makes two or three calls per order in a
# tight loop, which is the shape that finds an Azure APIM limiter. 0 disables our
# own pacing; a Retry-After from the gateway is obeyed either way.
#
# RANDOX'S DOCUMENTED LIMIT IS 600 A MINUTE, PER API (Chris Caulfield, Aug
# 2026), and the server refuses to boot with this set above it. 60 is a tenth of
# the ceiling on purpose: 600 is the rate at which they START REFUSING, so
# pacing at the refusal threshold means every ordinary burst discovers it.
# Nexus and Clinic Booking each get their own budget and neither spends the
# other's.
RANDOX_MAX_REQUESTS_PER_MINUTE=60
# Retries for TRANSIENT failures only — 429, 5xx, timeout, dropped
# connection. Never applied to CreatePendingOrder: a 502 says nothing about
# whether the order reached the laboratory, and a retried create is a second
# real order for a real patient.
RANDOX_RETRY_MAX_ATTEMPTS=3
# Exponential from here, with jitter, capped at 60s. Jitter matters because
# the poll sweep is a loop: without it a Randox blip re-synchronises every
# order onto the same retry instant.
RANDOX_RETRY_BASE_DELAY_MS=500

# --- Dev/test only — NEVER true anywhere internet-reachable ---
# Returns the raw 2FA code in the login API response so local dev and e2e
# tests don't need to read it from the email provider log. Leaving this
# true in a real deployment is a full 2FA bypass.
EXPOSE_DEV_OTP_CODE=false

# --- Demo data (prisma/seedDemo.ts) ---
# SYNTHETIC patient + 4 released reports, for showing the portal before real
# patients exist. When true, the API container re-seeds it on every boot
# (idempotently, re-anchoring the report dates to "now"); unset or false and
# the boot-time call is a silent no-op. This is also the explicit opt-in that
# lets the demo seed run at all under NODE_ENV=production.
# Turn OFF and run `npm run prisma:seed:demo -- --purge --allow-production`
# before the first real patient is onboarded.
SEED_DEMO_DATA=false
# The demo patient still has to pass 2FA, so wherever email is genuinely
# being sent this must be a mailbox you can actually open.
SEED_DEMO_EMAIL=demo.showcase@aspireshield.dev
SEED_DEMO_PASSWORD=

# --- Email verification (self-signup) ---
# How long the emailed six-digit confirmation code stays good for, in MINUTES.
# Short on purpose: it only has to survive someone switching to their inbox,
# and six digits is not enough entropy to leave standing for hours.
# Accepted range is 5-60; anything outside it fails validation at boot.
EMAIL_VERIFICATION_TTL_MINUTES=20

# --- Password reset ---
# How long a "reset your password" link stays good for, in MINUTES (not
# hours). Unlike a verification link, this one can take over an account, so
# its window is the time it takes to open an inbox and nothing more. Each
# link is single-use, issuing a new one spends any link still outstanding,
# and using one revokes every live session on the account.
PASSWORD_RESET_TTL_MINUTES=60

# --- Rate limiting / login lockout ---
# Windows are in SECONDS, not minutes — the login window is now short enough
# that minutes can't express it.
#
# The lockout stays: it's the only thing standing between someone and
# brute-forcing patient records. But the old 5-in-15-minutes fired on
# ordinary human mistyping and shut real patients out of their own results
# for a quarter of an hour. Ten attempts inside two minutes is far below any
# useful guessing rate, effectively unreachable by hand, and the counter is
# cleared outright on any successful sign-in — so a person who fumbles their
# password and then gets it right doesn't start their next sign-in halfway
# to a lock. Both numbers are tunable here without a deploy.
LOGIN_RATE_LIMIT_MAX=10
LOGIN_RATE_LIMIT_WINDOW_SECONDS=120
# Deliberately NOT loosened alongside login. A six-digit code has a search
# space of one million, and whoever reached this step already holds a valid
# password — a generous allowance here is a materially different risk.
OTP_RATE_LIMIT_MAX=5
OTP_RATE_LIMIT_WINDOW_SECONDS=900
# "Send me a new code" has its own budget, separate from OTP verification:
# resend causes an outbound email/SMS, and a patient legitimately asking for
# a second code must not thereby lose the attempts they need to enter it.
# The per-attempt cooldown (30s) and reissue cap (3) in auth/service.ts are
# the primary control; this is the backstop.
OTP_RESEND_RATE_LIMIT_MAX=4
# Registration is open to anyone (patients register themselves at /signup),
# so this is an anti-abuse ceiling on how many accounts one connection can
# create — not a gate on who may register.
SIGNUP_RATE_LIMIT_MAX=10
SIGNUP_RATE_LIMIT_WINDOW_SECONDS=3600

# --- Web app (Vite build-time) ---
# The clinic's public phone number. Every "we couldn't get you signed in,
# call us" affordance in the app lights up when this is set, and falls back
# to the clinical-team email address when it isn't. Deliberately has no
# hard-coded default: inventing a phone number for a real medical practice
# risks sending patients to a stranger. Set it to the practice's published
# number in the Vercel environment before go-live.
VITE_CLINIC_PHONE=
